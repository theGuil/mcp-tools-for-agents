{
  "mcpServers": {
    "media": {
      "command": "uv",
      "args": ["run", "--directory", ".", "mcp-tools"],
      "env": {
        "WORKSPACE_DIR": "./workspace",
        "MCP_DOMAINS": "video,audio,files,jobs"
      }
    }
  }
}
