//! STUB: tool ainda não portada. Substituir pelo port completo.

use std::sync::Arc;

use crate::domains::{McpServer, Runtime};

/// Expõe a tool no servidor.
pub fn register(_mcp: &mut McpServer, _runtime: &Arc<Runtime>) {}
